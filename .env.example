# Copy this file to .env and adjust values for your local environment.
# The backend reads these via pydantic-settings (dotenv).

JOBS_DIR=./jobs
DB_PATH=./db/huddletag.db
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:5173
